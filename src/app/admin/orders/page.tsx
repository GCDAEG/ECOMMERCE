import React from "react";

interface PageProps {}

const Page: React.FC<PageProps> = ({}) => {
  return (
    <div className="min-h-screen w-full bg-gray100">
      <h1>Ordenes</h1>
    </div>
  );
};

export default Page;
