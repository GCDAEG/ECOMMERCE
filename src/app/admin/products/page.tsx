import Products from "@/components/ui/Products/Products";

const Page = async ({}) => {
  return (
    <div className="component-name flex flex-col">
      <Products />
    </div>
  );
};

export default Page;
