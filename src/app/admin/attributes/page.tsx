import Attributes from "@/components/ui/Attributes/Attributes";

const Page = ({}) => {
  return (
    <div className="w-min-full">
      {/* LISTA DE ATRIBUTOS */}
      <Attributes></Attributes>
    </div>
  );
};

export default Page;
